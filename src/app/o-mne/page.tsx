// src/app/o-mne/page.tsx

import { Container, Typography } from '@mui/material';

export const metadata = { title: 'O mne | Insta 2.0' }

export default function About() {
  return (
    <Container>
        <Typography> Stranka o mne </Typography>
    </Container>
  );
}
