// src/app/prispevok/page.tsx

import { Container, Typography } from "@mui/material";

export const metadata = { title: 'List of posts | Insta 2.0' };

export default function PostList() {
  return (
    <Container>
      <Typography> List of posts </Typography>
    </Container>
  );
}