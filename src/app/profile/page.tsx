// src/app/profil/page.tsx

import { Container, Typography } from "@mui/material";

export const metadata = { title: 'List of profiles | Insta 2.0' };

export default function ZoznamProfilov() {
  return (
    <Container>
      <Typography> List of profiles </Typography>
    </Container>
  );
}