// src/sections/HomeView.tsx

import { Container, Typography } from "@mui/material";

export const metadata = { title: 'Home | Insta 2.0' };

export default function HomeView() {
    return (
        <Container>
            <Typography> Home </Typography>
        </Container>
    );
}